# Claude Code Relay — .env.example
# Empty = use current process CWD. Windows paths are OK.
CODEX_WORKDIR=

# 0 = no hard cutoff (use idle-after-turn only)
CODEX_MAX_RUN_SEC=0

# Close stream N seconds after Codex emits turn.completed
CODEX_IDLE_AFTER_TURN_SEC=10

# Toggle verbose SSE pretty logs to console
RELAY_PRETTY_LOG=1
